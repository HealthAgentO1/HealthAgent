# Copy to `frontend/.env` or `frontend/.env.local` (not committed).

VITE_API_URL=http://127.0.0.1:8000/api

# Broadcast redirect uses the API (root .env BROADCAST_ADMIN_EMAIL). No Vite var required.
